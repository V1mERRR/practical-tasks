if __name__ == "__main__":
    # Do not change the line below
    a, b = input("Enter a: "), input("Enter b: ")

    # Convert inputs to integers (or floats, depending on your needs)
    a = int(a)
    b = int(b)

    # Modify variables a and b to make the script work without errors

    # Do not change the line below
    assert a * b  # This ensures that the result of a * b is not 0
